import {createElement as c, Fragment} from 'react';
import {Container, Form, Row, Col} from 'react-bootstrap';

export function Modules(){
    //const repos = useS(d=> d.repos.);
    return(
        c(Col, {},
            'modules'
        )
    )
}